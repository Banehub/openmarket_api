PORT=3000
MONGODB_URI=mongodb://localhost:27017/openmarket
JWT_SECRET=your-secret-here
CORS_ORIGIN=http://localhost:5173
# Optional: public URL of this API (no trailing slash). If unset, upload URLs are derived from the request host so same-origin works on Render.
# BASE_URL=https://openmarket-api-m2lz.onrender.com
# UPLOAD_DIR=./uploads
